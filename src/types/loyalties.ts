export type MyLoyaltyHistory = {
  title: string;
  description: string;
  date: string;
};

export type RewardsType = {
  id: number;
  rewardId: string;
  name: string;
  points: string;
  image: string;
  description: string;
  createdAt: string;
  updatedAt: string;
};
